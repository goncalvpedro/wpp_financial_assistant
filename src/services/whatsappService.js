const sendWhatsAppMessage = async (phone, message) => {
  console.log(`Sending to ${phone}: ${message}`);
  // TODO: integrate with whatsapp-web.js client
};

module.exports = { sendWhatsAppMessage };


